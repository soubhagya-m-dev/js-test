"use strict" //treat all js code as a newer version of js

// alert("Jello") //we are using node js, not browser 

//data types
/*
number => 2 to the power 53
bigint
string => ""
boolean => true/false
null => standalone value, value doesn't come
undefined => value not defined
symbol => unique
*/
let age = 15
let names = ""
let isLoggedIn = false
let temp = null
let state
console.table([age, names, isLoggedIn, temp, state])

console.log(typeof null) // type is object - interview qs.
console.log(typeof undefined) // type is undefined